# Apps

Target application layout:

- `admin/`: web admin for organizations, projects, campaigns, subscribers, stats, and integrations.
- `api/`: Node.js TypeScript API service.
- `worker/`: push delivery worker.
- `scheduler/`: scheduled campaign dispatcher.

The imported Raschini prototype currently lives at the repository root. It will be moved or split after the architecture is approved.
