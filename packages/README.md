# Packages

Target shared packages:

- `sdk/`: embeddable browser SDK and service worker template.
- `shared/`: shared TypeScript types, validation schemas, and constants.
- `database/`: migrations, seed data, and database helpers.

Keep package APIs tenant-aware from the first implementation stage.
