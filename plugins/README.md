# Plugins

Target plugin/module sources:

- `wordpress/`: WordPress plugin source and ZIP build pipeline.
- `bitrix/`: 1C-Bitrix module source and packaging docs.

Both integrations should use the public SDK and API instead of duplicating push logic.
